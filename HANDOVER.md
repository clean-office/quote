# 견적 대시보드 — 인수인계 (다른 기기에서 이어서 개발용)

> 새 세션의 Claude에게: 이 문서가 곧 인수인계다. 다 읽으면 사용자에게 긴 설명 요구하지 말고 바로 이어서 작업할 것.
> 앱 소스는 이 저장소의 `index.html` 하나가 전부다. 원문: https://raw.githubusercontent.com/clean-office/quote/main/index.html

## 1. 이게 뭔가
- 사용자(성원)는 인테리어 실측·영업을 하는 **모두랩(moredolab)** 운영자. 밖에서 폰으로 견적서를 만들어 고객에게 카톡으로 보내는 용도.
- 회사 엑셀 견적 양식("템플릿 1")을 웹앱으로 옮긴 것. 시트 3장 구성: **갑지**(표지: 로고+명함+금액) / **집계표**(공종별 합계+간접비) / **내부내역**(품목 상세).
- 1단계 완료·배포됨: https://clean-office.github.io/quote/ (GitHub Pages, repo `clean-office/quote`, 브라우저 로그인으로 운영 — 별도 토큰 없음)

## 2. 양식 규칙 (변경 금지, 사용자 확인 없이 바꾸지 말 것)
- 계산: 품목금액 = 수량×재료단가 + 수량×노무단가 → 공종 소계 → 직접공사비
- 간접비 = 직접공사비 × (공사경비 2.5% + 공과잡비 2.5% + 기업이윤 5%) — 각각 개별 계산·표시, 요율은 UI에서 조정 가능
- 합계 = 직접 + 간접 − 단수정리. 단수정리는 기본 만원 미만 절사, "목표 총액" 입력 시 그 금액에 정확히 맞춤
- 갑지 금액 표기: "일금 ○○○원정 / 1,234,000" (한글 변환 함수 hangulNum 내장)
- 갑지 상단 좌측 로고, 우측 하단 명함 이미지 — index.html 안에 base64로 내장돼 있음 (LOGO_B64, CARD_B64 상수)
- 내보내기 파일명: `{공사명}_1_갑지.jpg` / `_2_집계표` / `_3_내부내역`, 엑셀은 `{공사명} 견적서.xlsx`
- V.A.T 별도가 기본 견적조건

## 3. index.html 구조 (단일 파일, ~2400줄)
- 에디터 UI: 기본정보 → 공종·품목(동적 행) → 간접비·단수정리 → 텍스트 붙여넣기 임포트 → 내보내기 버튼 → 미리보기(sheet 3장, 1100px 고정폭을 transform scale로 모바일 대응)
- 상태: 전역 `state` 객체 {title, client, date, terms, r1, r2, r3, target, sections:[{name, items:[{name,spec,unit,qty,mat,lab}]}]}
- 주요 함수: calc()(합계), buildPreview()(시트 HTML), exportJPG()(html2canvas), exportXLSX()(ExcelJS, 수식 포함 3시트+이미지), importText()(텍스트 파싱), autosave/saveNamed/openSaves(localStorage: mdl_draft, mdl_saves)
- 외부 라이브러리: cdnjs의 html2canvas 1.4.1, ExcelJS 4.4.0 (CDN 로드)
- 텍스트 임포트 형식: `공사명: …` / `제출처: …` / `[공종명]` / `품명 / 규격 / 단위 수량 / 재료 15,000 / 노무 20,000` / `총액: 730만`

## 4. 다음 할 일 (2단계 백로그 — 사용자와 우선순위 확인)
1. Supabase 연동: 견적 저장·불러오기 기기 간 동기화 (프로젝트는 이미 있음 — URL·anon 키는 사용자 PC의 `클린 오피스/00_프로젝트_기록/키모음_비공개.md`에 있음. **키를 이 공개 repo에 절대 넣지 말 것.** 앱에서는 localStorage에 키 저장하는 기존 클린오피스 앱 방식 참고)
2. 로그인(비밀번호 게이트 또는 Supabase Auth) — 지금은 주소 아는 사람 누구나 열람 가능
3. 단가·품목 사전(자주 쓰는 품목 탭 한 번에 추가)
4. 사용자 폰 실사용 피드백 반영 (JPG/엑셀 다운로드가 폰 브라우저에서 잘 되는지 미확인)

## 5. 배포 방법 (수정 후)
- github.com에 브라우저 로그인(계정 clean-office) → repo `quote` → index.html 열기 → 연필(Edit) → 내용 교체 → Commit → 1~2분 뒤 반영
- 또는 Upload files로 덮어쓰기. Pages 설정은 이미 켜져 있음(main / root)
- Claude가 크롬 확장으로 직접 커밋 가능 (이전 세션에서 그렇게 배포함)

## 6. 사용자 작업 스타일 (중요)
- Claude가 할 수 있는 건 전부 Claude가 직접 한다 (파일 처리·브라우저 조작·배포 포함). 사용자에게 "해보세요" 금지 — 로그인·비밀번호 입력만 요청
- 결과물이 웹페이지면 URL만 주지 말고 크롬으로 띄워서 보여줄 것
- 응답은 간결하게. 완료 후 이 문서(HANDOVER.md)도 최신 상태로 갱신해서 repo에 같이 커밋할 것
